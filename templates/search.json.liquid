{% layout none %}
{% capture results %}
  {% for item in search.results %}
    {% assign product = item %}
		{{ product.id }}
    {% unless forloop.last %},{% endunless %}
  {% endfor %}
{% endcapture %}
{
	{{ results }}
}